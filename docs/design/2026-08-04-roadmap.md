# 實作路線圖

日期：2026-08-04（2026-08-05 更新：Phase 0、Phase 1 皆完成並通過驗證）
狀態：Phase 0、Phase 1 已完成，Phase 2（加入本地翻譯）待開始

依 [feasibility](2026-08-04-feasibility.md) 與 [architecture](2026-08-04-architecture.md) 文件展開的分階段計畫。每個階段結束都應該有一個可實際操作、可驗證的產出，而不是只有程式碼。

---

## Phase 0 — 技術驗證 Spike（先做，不寫產品程式碼）✅ 已完成（2026-08-05）

**目的**：驗證 feasibility 文件裡最大的未知數 — WhisperKit 串流 + MLX Gemma 4 能否在 M4 / 16GB 上同時跑，延遲是否真的「快速」。

- [x] 建一個最小 Swift command-line 測試：檔案音訊 → WhisperKit 轉錄，量測延遲（`spike/Sources/WhisperSpike`）— 20-74x 即時速度，見 [結果](2026-08-04-phase0-spike-results.md)
- [x] 獨立測試 MLX Gemma 4 E4B 與 E2B 的單句翻譯延遲，記錄 tokens/sec 與首字延遲（`spike/gemma-spike`）— E2B 暖機後 0.35-0.43s/句，見 [結果](2026-08-05-phase0-spike-results-translation.md)
- [x] 兩者同時載入，量測記憶體峰值 — 合計 ~1.9GB，16GB 機型無虞
- [x] 用 Whisper 直接轉錄中文語音，確認簡體/繁體傾向 — 強制指定 `language: "zh"` 後直接輸出繁體，OpenCC 降級為保險層而非必要項
- [x] 產出：三份量測結果文件，並回頭修正了 [feasibility 文件](2026-08-04-feasibility.md)（翻譯預設模型由 E4B 改為 E2B、記憶體估算改用實測值、簡繁體與記憶體壓力風險項解除）

**結論：通過**，全部指標遠優於門檻，沒有觸發「不通過的訊號」。唯二遺留到 Phase 1 的開放問題：語言自動偵測穩定性（合成音訊上會誤判）、中英夾雜場景品質（尚未用真人錄音測試）。

### 換機器接續開發前必看：環境需求

Phase 0 踩了不少建置環境的坑，換一台新機器前務必確認：

1. **必須裝完整 Xcode.app**（不是只有 Command Line Tools）— MLX Swift 需要 `xcrun metal` 編譯自訂 Metal shader，這個工具只隨完整 Xcode 附帶。目前用的是 Xcode 16.4，相容 macOS 14.5+，不需要升級到 macOS 26 Tahoe。
2. **MLX 相關程式碼必須用 `xcodebuild` 建置，不能用 `swift build`**：純 SPM 建置雖然會成功，但執行時會噴 `Failed to load the default metallib`（Metal shader 封裝是 Xcode build phase，不是 SPM plugin）。WhisperKit 那邊沒有這個限制，純 `swift build` 就能跑。
3. 對本機 `Package.swift` 資料夾直接下 `xcodebuild -scheme <package-name> -configuration Release -destination "platform=macOS,arch=arm64" -derivedDataPath .build/xcode -skipMacroValidation build` 即可，Xcode 16 會自動抓套件名稱當 scheme，不需要另外建 `.xcodeproj`。
4. `@main` 進入點檔案**不能命名為 `main.swift`**（`xcodebuild` 會報 `'main' attribute cannot be used in a module that contains top-level code`，`swift build` 反而不會報這個錯——兩者對這個組合的容許度不同）。
5. 模型下載快取位置：WhisperKit 用 `~/Documents/huggingface/`，MLX/Gemma 4 用 `~/.cache/huggingface/`（兩者路徑不同，不是同一個快取）。換機器要重新下載，Gemma 4 E2B 3.3GB + WhisperKit small ~465MB，抓好時間與網路。
6. 詳細除錯過程與踩坑細節都記在 [2026-08-05 翻譯 spike 結果文件](2026-08-05-phase0-spike-results-translation.md) 的「環境發現」章節。

---

## Phase 1 — 音訊擷取 + 純 STT（先不翻譯）✅ 已完成並通過人工驗證（2026-08-05）

- [x] 移植 `AudioCaptureManager.swift`（原封不動，複製自原專案）
- [x] 實作 `LocalSTTEngine.swift`：固定長度（3 秒）分塊緩衝 + WhisperKit 逐塊轉錄。**已知取捨**：不是原本設想的串流式轉錄（WhisperKit 的 `AudioStreamTranscriber` 是為麥克風輸入設計的，`AudioCaptureManager` 這種外部 App 音訊來源接不上去），改成固定分塊，代價是句子可能在分塊邊界被切斷，之後可視實測效果再優化成 VAD 動態分塊
- [x] 最簡 UI（`ContentView.swift`）：模型選擇（tiny/base/small）+ App 選單 + 開始/停止 + 原文字幕捲動列表
- [x] `app/build_app.sh`：比照原專案的 `swiftc` + 手動打包模式，改成 `swift build -c release` + 打包（Phase 1 只需要 WhisperKit，純 `swift build` 就夠，不需要 `xcodebuild`）
- [x] 建置驗證：`swift build -c release` 成功、打包成 `.app` 並 ad-hoc 簽名成功、啟動後 UI 正常渲染、App 選單正確列出並自動選中 Google Chrome、螢幕錄音權限已出現在系統設定清單中
- [x] **人工驗證（使用者實測）**：點擊「開始」→ 目標 App 播放音訊 → 即時看到原文字幕出現 — **確認可行**

**產出**：`app/` 目錄下可建置執行的 App，端到端「選擇 App → 開始 → 即時看到原文字幕」流程已由使用者實機確認可用。

**使用者實測回饋（2026-08-05）**：固定 3 秒分塊確實會明顯感覺到句子被切斷、讀起來不通順，跟上面「已知取捨」預期的一致，但影響比預期明顯。

**分塊策略優化（2026-08-05，已完成程式碼修改，待人工重新驗證）**：`LocalSTTEngine` 改成 VAD（語音活動偵測）驅動的分塊，不再是固定時間切：
- 直接重用 WhisperKit 內建的 `EnergyVAD`（`Sources/WhisperKit/Core/Audio/EnergyVAD.swift`），不用另外實作能量偵測邏輯，也不依賴 `AudioStreamTranscriber` 的麥克風擷取部分（它是純陣列分析工具，沒有綁定音訊來源）
- 每次收到新音訊後，檢查緩衝區最後 0.35 秒是否為靜音（`EnergyVAD.voiceActivity(in:)` 全部回傳 false）；偵測到停頓且緩衝區至少有 0.8 秒內容時才切（避免太短的停頓切出單字片段）
- 加了 8 秒的強制切分上限，避免使用者講話完全不停頓時字幕卡住不出現
- 已知取捨：VAD 的預設能量閾值（`energyThreshold: 0.02`）是針對一般語音調的，ScreenCaptureKit 擷取到的 App 音訊（例如 Zoom/Chrome 的混音輸出）音量特性可能不同，實際效果如何、閾值需不需要調整，都要靠人工重新測試才知道

已重新打包成 `.app`（因為重新簽名，螢幕錄音權限會被系統重置，第一次開啟需要重新授權）。

**人工重新驗證（2026-08-05，使用者實測）**：確認句子不太會被切斷了，VAD 停頓偵測有效。分塊策略優化到此告一段落，**Phase 1 正式完成**，開始進入 Phase 2。

---

## Phase 2 — 加入本地翻譯

- [ ] 實作 `LocalTranslationEngine.swift`（MLX Gemma 4）
- [ ] 移植句子邊界偵測邏輯（`checkAndRotateSubtitle`）觸發翻譯
- [ ] 實作 `OpenCCConverter.swift`，套用在 STT 與翻譯兩個輸出節點
- [ ] UI 加回雙語對照顯示（原文 + 繁中翻譯），比照原專案 `showBilingual` 開關
- [ ] 產出：完整的即時雙語字幕體驗，離線可用，這是本專案的 MVP 核心

---

## Phase 3 — UX 移植：懸浮字幕 + 逐字稿匯出

- [ ] 移植 `FloatingSubtitleWindow.swift`（原封不動）
- [ ] 移植逐字稿自動匯出邏輯（`exportTranscript`）
- [ ] 產出：與原專案體驗一致的懸浮字幕 + 停止後自動存 Markdown 逐字稿

---

## Phase 4 — 會議筆記編輯器 + 網頁匯出

- [ ] 移植 `MarkdownRenderer.swift` / `MeetingNotesEditor.swift` / `NotesPreviewView.swift`（原封不動）
- [ ] 移植三欄式版面與 session timestamp 管理邏輯
- [ ] 移植網頁匯出流程（`exportWebPage`）
- [ ] 產出：使用者可在會議中手寫筆記、貼圖，結束後產生單一 HTML 會議記錄網頁

---

## Phase 5 — 本地會議摘要（取代 Gemini 摘要）

- [ ] 實作 `LocalSummaryService.swift`，用同一顆 Gemma 4（長 context）產生 `MeetingMinutes`（摘要／重點／action items）
- [ ] 移植 `MeetingMinutesView.swift`
- [ ] 驗證：逐字稿過短時跳過摘要、API（模型推論）失敗時不影響已存檔的逐字稿 — 這兩條原專案的硬性原則要延續
- [ ] 產出：功能對齊原專案的完整本地版 App

---

## Phase 6 — Stretch Goals（明確排除於 MVP 之外）

- [ ] 本地語音配音（TTS 同聲傳譯播放），評估導入 `argmax-oss-swift` 的 `TTSKit`
- [ ] 模型下載管理 UI（`ModelManager` 的圖形化介面：進度、切換模型大小、清除快取）
- [ ] Apple `SpeechAnalyzer`（macOS 26+）作為可選的 STT 加速後端
- [ ] 說話者辨識／分軌（原專案的 spec 文件中也明確排除，屬於更大範圍的功能）

---

## 里程碑判斷準則

每個 Phase 完成的定義是「可以實際操作看到功能運作」，不是程式碼寫完但沒驗證。沿用原專案的驗證方式（專案沒有 XCTest，靠手動跑情境）：至少要跑過一次「正常流程」「邊界情況（極短會議）」「錯誤情況（模型載入失敗／記憶體不足）」三種情境，且錯誤情況不能讓 App crash 或遺失已完成的逐字稿。
